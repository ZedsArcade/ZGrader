{% extends "base.html.jinja" %}
{% block content %}
  <p>{{ strings.greeting }}</p>
  <p>
    {{ strings.received_intro }} <strong>{{ submission_code }}</strong>
    {% if card_name %}{{ strings.received_for }} <strong>{{ card_name }}</strong>{% endif %}.
  </p>
  <p>{{ strings.received_body }}</p>
{% endblock %}

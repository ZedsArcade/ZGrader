# Copy this file to .env and fill in real values before running
# `docker compose up`. See docs/qa_checklist.md for the full setup walkthrough
# and docs/deployment.md for the Cloudflare Tunnel notes.

# --- Environment ---
# `production` turns on the startup checks that refuse the shipped-default
# secret and database password, and switches off the interactive API docs at
# /api/docs. The backend will not boot in production until the two values
# below are real -- that is deliberate. Use `development` only for local work.
ZGRADER_ENV=production

# --- Postgres ---
POSTGRES_USER=zgrader
# Generate one, e.g.: python3 -c "import secrets; print(secrets.token_urlsafe(24))"
POSTGRES_PASSWORD=change-me-to-a-real-password
POSTGRES_DB=zgrader
POSTGRES_DATA_PATH=/mnt/user/appdata/zgrader/postgres
REPORTS_DATA_PATH=/mnt/user/appdata/zgrader/reports

# Host-side path the scanner software writes into (SMB/NFS mount point, or a
# local folder). Mounted into both `backend` and `worker` at /data/scans.
# On Unraid this must be writable by uid:gid 99:100 (nobody:users), which is
# what the containers run as -- see backend/Dockerfile's APP_UID/APP_GID.
SCANS_HOST_PATH=/mnt/user/appdata/zgrader/scans

# --- Backend ---
# Generate a real random value: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# Changing it later logs everyone out; it signs every session token.
ZGRADER_SECRET_KEY=change-me-to-a-real-random-secret

# Public origin customers reach the site on. Used to build the links inside
# verification and password-reset emails, so a wrong value here produces mail
# with links nobody can open. Include the scheme, no trailing slash.
ZGRADER_SITE_URL=https://cards.example.com

# --- Google sign-in (optional) ---
# Both must be set or the feature stays off: `google_enabled` is derived as
# bool(client_id and client_secret), and GoogleSignInButton renders nothing
# until the backend reports it enabled -- so a half-configured deployment
# shows no button rather than one that dead-ends.
#
# THE REDIRECT URI IS DERIVED, NOT CONFIGURED. `auth/google.py` builds it as
# <ZGRADER_SITE_URL>/api/auth/google/callback, and Google matches the string
# exactly -- a trailing slash, a http:// where the site is https://, or a
# stale ZGRADER_SITE_URL all fail with redirect_uri_mismatch. With the value
# above that is:
#
#     https://cards.example.com/api/auth/google/callback
#
# Register that in the Google Cloud console under the OAuth 2.0 Client ID's
# "Authorized redirect URIs". It goes through the site origin rather than the
# backend's own address because the browser reaches the API through the
# Next.js /api rewrite. For local testing add a second URI,
# http://localhost:3000/api/auth/google/callback -- Google allows plain http
# for localhost only.
#
# The scope requested is "openid email" and nothing else, no provider tokens
# are stored, and an unverified Google address is refused. Note that signing
# in with Google against an address that already has a password account is
# *refused rather than linked*, deliberately: linking on a matching address
# is an account-takeover route. See the callback in api/routers/auth.py.
# ZGRADER_GOOGLE_CLIENT_ID=1234567890-abcdefg.apps.googleusercontent.com
# ZGRADER_GOOGLE_CLIENT_SECRET=

# --- Backups ---
# Where the nightly dumps land. Put this on a DIFFERENT disk from
# POSTGRES_DATA_PATH -- a backup on the same array dies with the array, which
# is one of the two things it exists to survive.
#
# The backup service dumps the database AND archives the reports and scans
# directories, because only the first is in Postgres. See docs/backup.md for
# the restore drill; an untested backup is a belief rather than a backup.
BACKUP_HOST_PATH=/mnt/user/backups/zgrader
BACKUP_RETENTION_DAYS=14
# 86400 = daily. Lower it while you are testing the setup, then put it back.
BACKUP_INTERVAL_SECONDS=86400

# --- Cloudflare Tunnel (the reference way to publish the site) ---
# Create the tunnel in Cloudflare Zero Trust -> Networks -> Tunnels, choose
# Docker as the environment, and copy the token out of the command it shows
# (the long eyJ... string after --token). Route the tunnel's public hostname
# to http://caddy:80 -- cloudflared runs in this stack, so it reaches Caddy
# over the Docker network with no host port involved.
#
# The cloudflared service starts unconditionally. Without a token it logs the
# missing credential and restarts; if you are not using a tunnel, delete the
# service from docker-compose.yml.
#
# Nothing is port-forwarded at the router, and nothing should be: the backend
# trusts CF-Connecting-IP for rate limiting, so a directly reachable origin
# lets anyone forge it. See docs/deployment.md.
# CLOUDFLARE_TUNNEL_TOKEN=

# --- Admin bootstrap (optional) ---
# Set both to auto-create an operator/admin account on startup (the admin
# panel lives at /admin). If an account with this email already exists as a
# client, it is promoted to operator -- keeping its existing password, because
# a redeploy silently reverting a password you set in the app would be a
# miserable thing to debug. Case doesn't matter; addresses are normalised.
# ZGRADER_ADMIN_EMAIL=you@example.com
# ZGRADER_ADMIN_PASSWORD=<a strong password>

# Locked out? Set this to true and redeploy: the password above is forced onto
# the account and every existing session for it is signed out. Then unset it
# and redeploy again -- while it stays true, anyone who can read this file can
# take the admin account on the next restart.
# ZGRADER_ADMIN_RESET_PASSWORD=false

# --- Optional external AI vision model (off by default) ---
# When enabled, the analysis pipeline also asks an Ollama-compatible vision
# model for extra "AI-assisted" observations. Leave disabled unless you host
# one (e.g. an Ollama container with a llava/qwen2-vl model).
#
# PRIVACY: this POSTs the customer's card image to whatever ZGRADER_AI_ENDPOINT
# names. The published privacy policy says nobody but the operator sees those
# images and that they are not used to train anything, which stays true only
# while the endpoint is a model you host yourself. Pointing this at a hosted
# API makes that page false and adds a processor that has to be named on it.
# ZGRADER_AI_ENABLED=false
# ZGRADER_AI_ENDPOINT=http://ollama:11434/api/generate
# ZGRADER_AI_MODEL=llava

# How long a contact-form enquiry is kept before the worker deletes it.
# contact_messages has no foreign key to users -- the sender need not have an
# account -- so account deletion cannot reach it and this sweep is the only
# thing that ever removes a name, an address, a message body and an IP.
# The privacy policy quotes this figure, so changing it means changing that
# page too. 0 disables the sweep and keeps enquiries indefinitely.
ZGRADER_CONTACT_MESSAGE_RETENTION_DAYS=365

# --- SMTP ---
# READ THIS BEFORE GOING LIVE. The defaults below point at the bundled
# `mailhog` dev service, which only runs under the `dev` compose profile -- so
# in a normal production deployment they point at nothing and no mail leaves
# the server at all.
#
# That is not cosmetic. Submitting a card requires a confirmed email address,
# and the only way to confirm one is the link in the verification email. With
# no working relay, a customer can register and then find they cannot submit
# anything, with no way to fix it themselves. Password reset is dead too.
#
# Point these at a real relay before anyone else uses the site.
#
# Don't self-host the outbound side. Delivering mail directly means connecting
# to each recipient's server on port 25, and consumer ISPs filter outbound 25
# nearly universally -- measured from this deployment's network, the TCP
# handshake completes and then no SMTP banner ever arrives, against Google and
# Outlook alike. Consumer IP ranges also sit on Spamhaus' PBL by default and
# rDNS/PTR usually cannot be set, so even an open port 25 lands in spam.
#
# The shape that works, and costs nothing:
#   receiving -> Cloudflare Email Routing, free, forwards you@yourdomain to an
#                inbox you already read. Set the result as contact_email in
#                Admin -> Settings.
#   sending   -> a transactional relay's free tier (Resend, Brevo, MailerSend).
#
# Send from a SUBDOMAIN (e.g. noreply@send.yourdomain). Two reasons: a domain
# may have exactly one SPF record and two is a permanent failure rather than a
# merge, so keeping the relay's SPF on a subdomain stops it colliding with the
# one Cloudflare Email Routing writes to the root; and if the relay's
# reputation ever suffers, the damage is contained to the subdomain.
#
# TLS: the two flags below are alternatives, not layers.
#   port 587 -> ZGRADER_SMTP_USE_TLS=true      (STARTTLS)
#   port 465 -> ZGRADER_SMTP_IMPLICIT_TLS=true (TLS from the first byte)
# Setting USE_TLS on 465 hangs until timeout: there is no plaintext greeting
# to upgrade. The app warns about that pairing at startup.
#
# Verify without registering a throwaway account: Admin -> Settings -> send a
# test email. It reports the actual SMTP result rather than swallowing it.
ZGRADER_SMTP_HOST=mailhog
ZGRADER_SMTP_PORT=1025
ZGRADER_SMTP_USER=
ZGRADER_SMTP_PASSWORD=
ZGRADER_SMTP_FROM=noreply@zgrader.local
ZGRADER_SMTP_USE_TLS=false
ZGRADER_SMTP_IMPLICIT_TLS=false

# --- Capacity -------------------------------------------------------------
#
# confirm-crop runs the whole OpenCV pipeline inside the API request, so
# without a cap a burst of submissions saturates the API workers and degrades
# everything else sharing the box. This bounds the API's share; the worker
# container adds one more analysis on top of it.
#
# A request that arrives with every slot taken gets 503 with Retry-After
# rather than queueing -- a wait nobody can see is worse than a clear refusal.
ZGRADER_MAX_CONCURRENT_ANALYSES=2

# Threads each analysis may use. Without this the cap above limits how many
# analyses run while each still fans out across every core, which is a cap
# that does not mean what it looks like.
#
# Measured on a 4000x3000 photograph: the work stops scaling at about 4
# threads, so holding it to 2 costs roughly 11% and buys a predictable share
# of the machine. On an 8-core box, 2 analyses at 2 threads plus the worker's
# 1 leaves room for Postgres and whatever else Unraid is running.
#
# docker-compose.yml also passes this as OMP_NUM_THREADS and
# OPENBLAS_NUM_THREADS, because NumPy's bundled OpenBLAS is a separate pool
# that reads those at import time and ignores anything the app sets later.
ZGRADER_ANALYSIS_THREADS=2

# --- Offsite backups ------------------------------------------------------
#
# The nightly backup already writes the database, the reports and the scans to
# BACKUP_HOST_PATH. That copy dies with the machine, which is one of the two
# things a backup is for -- these settings send an encrypted copy somewhere
# else.
#
# Leave BACKUP_OFFSITE_REMOTE empty and nothing is sent: the step logs one line
# and does nothing. That is a supported state, not a broken one, so this can be
# deployed before the destination exists.
#
# Any rclone remote works, which is what keeps the destination a configuration
# question rather than a code change. The simplest start on Unraid is a disk
# outside the array, mounted with the Unassigned Devices plugin:
#
#   1. mount it at, say, /mnt/disks/zgrader-backup
#   2. OFFSITE_HOST_PATH=/mnt/disks/zgrader-backup
#   3. RCLONE_CONFIG_OFFSITE_TYPE=local
#   4. BACKUP_OFFSITE_REMOTE=offsite:/offsite
#
# That survives the array failing and a bad delete. It does not survive fire or
# theft -- for that, either rotate two disks and keep one off site, or add a
# second remote (a bucket, another machine) later. Nothing here changes.
BACKUP_OFFSITE_REMOTE=
OFFSITE_HOST_PATH=

# The PUBLIC half of an age key pair. Generate one OFF this machine:
#
#     age-keygen -o zgrader-backup-key.txt
#
# That file contains the private key. Put it in a password manager, or print
# it. Do not leave it on the server: the whole point is that this box can write
# backups it cannot itself read, so someone who takes the machine gets
# ciphertext. Losing the private key means losing every offsite backup, so
# treat it like the only copy it is.
#
# Paste the "public key:" line here. Without it the offsite step refuses to
# send rather than putting customer scans and a dump containing email addresses
# and password hashes into someone else's storage in the clear.
BACKUP_AGE_RECIPIENT=

# Longer than the local window on purpose: offsite protects against failures
# noticed slowly. This number is stated in the privacy policy -- changing it
# means changing that page too.
BACKUP_OFFSITE_RETENTION_DAYS=30

# rclone takes its whole configuration from the environment, so there is no
# config file to mount and no secret in this repo beyond this file, which is
# gitignored. Only set what the chosen backend needs.
RCLONE_CONFIG_OFFSITE_TYPE=
RCLONE_CONFIG_OFFSITE_ACCOUNT=
RCLONE_CONFIG_OFFSITE_KEY=
RCLONE_CONFIG_OFFSITE_HOST=
RCLONE_CONFIG_OFFSITE_USER=
RCLONE_CONFIG_OFFSITE_PASS=
